apiVersion: apps/v1
kind: Deployment
metadata:
  name: fixora-imagepull-demo
  labels:
    app: imagepull-demo
  annotations:
    fixora.io/repo-url: "https://github.com/baka126/fixora-demo.git"
    fixora.io/repo-path: "deployment-imagepull.yaml"
    fixora.io/vcs-type: "github"
    fixora.io/target-revision: "main"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: imagepull-demo
  template:
    metadata:
      labels:
        app: imagepull-demo
    spec:
      containers:
      - name: typo-container
        image: nginx:1.25-typo-non-existent
